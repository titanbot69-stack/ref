cto_row = next((r for r in hrms_rows_all if (r.get('external_designation') or '') == 'Chief Technology Officer'), None)
        ciso_row = next((r for r in hrms_rows_all if (r.get('external_designation') or '') == 'Chief Information Security Officer'), None)
        cto_email = (cto_row or {}).get('business_email')
        ciso_email = (ciso_row or {}).get('business_email')
