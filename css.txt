	{% if is_admin or external_designation == 'Chief Technology Officer' %}

    user_email = (request.user.email or '').lower()
    hrms_row = next((r for r in get_hrms_list() if (r.get('business_email') or '').lower() == user_email), None)
    external_designation = (hrms_row or {}).get('external_designation', '')
    if not (user_email == 'admin@test.com' or external_designation == 'Chief Technology Officer'):

        user_role_label = 'Admin' if user_email == 'admin@test.com' else 'CTO'
 
