    /* search box styling now centralized in dashboard.css; remove local overrides */
    .request-item{transition:box-shadow .2s, transform .15s;}
    .request-item:hover{box-shadow:0 6px 14px rgba(0,0,0,.12);transform:translateY(-2px);}
