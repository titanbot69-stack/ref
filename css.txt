            # Include employee department (role) for uniqueness & traceability
            dept = hrms_row.get('role') or hrms_row.get('internal_designation') or ''
            dept_part = f" ({dept})" if dept else ''


                f"for {hrms_row.get('business_email')}{dept_part} on {app_name} application"
