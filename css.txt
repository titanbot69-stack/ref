    const confirmRejectBtn = document.getElementById('app-owner-confirm-reject-btn');


    const remarkInput = document.getElementById('app-owner-reject-remark');
    const remarkCount = document.getElementById('app-owner-reject-remark-count');


        function syncRejectEnable(){
            const text=(remarkInput?.value||'').trim();
            if(remarkCount) remarkCount.textContent=String(text.length);
            if(confirmRejectBtn) confirmRejectBtn.disabled = !(ackBox?.checked && text.length>0 && text.length<=150);
        }
        if(ackBox){ ackBox.addEventListener('change', syncRejectEnable); }
        if(remarkInput){ remarkInput.addEventListener('input', syncRejectEnable); }


                body: JSON.stringify(Object.assign({ event_id: eventId, action: action }, action==='reject'?{ remark: (remarkInput?.value||'').trim() }:{}))


                if(remarkInput){ remarkInput.value=''; }
                if(remarkCount){ remarkCount.textContent='0'; }


                const text=(remarkInput?.value||'').trim();
                if(!text || text.length>150){ return; }
 
