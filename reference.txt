def help_page(request):
    """Corporate help & support page with fixed scrollable container"""
    return render(request, 'help.html', {'minimal_header': True})

def privacy_page(request):
    """Corporate privacy & data protection page with fixed scrollable container"""
    return render(request, 'privacy.html', {'minimal_header': True})
