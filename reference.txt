    email = (user.email or '').lower()

    # Determine roles
    # Admin rule: external_designation == 'Chief Technology Officer' OR legacy hard-coded admin email
    hrms_rows = get_hrms_list()
    hrms_row = next((r for r in hrms_rows if (r.get('business_email') or '').lower() == email), None)
    external_designation = (hrms_row or {}).get('external_designation', '') or ''
    is_admin = email == 'admin@test.com' or external_designation == 'Chief Technology Officer'
    is_ra = email in {ra.lower() for ra in RA_EMAILS}
    app_owner_emails = {o['email_id'].lower() for o in application_owners}
    is_app_owner = email in app_owner_emails

    tiles = []
    if is_admin:
        tiles.append({
