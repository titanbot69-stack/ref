   # Determine if this RA also holds CTO or CISO designation (executive approver)
    hrms_row = next((r for r in all_rows if (r.get('business_email') or '').lower() == email), None)
    external_designation = (hrms_row or {}).get('external_designation', '')
    show_access_requests = external_designation in ['Chief Technology Officer', 'Chief Information Security Officer']
 
